'use client';
import React from 'react';

// Wire @tanstack/react-query here later if/when needed.
export function QueryProvider({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
